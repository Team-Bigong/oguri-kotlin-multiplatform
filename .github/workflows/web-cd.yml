name: web-cd

on:
  workflow_run:
    workflows:
      - web-ci
    types:
      - completed

jobs:
  deploy:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'release-web'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger web deploy hook
        env:
          RENDER_WEB_DEPLOY_HOOK_URL: ${{ secrets.RENDER_WEB_DEPLOY_HOOK_URL }}
        run: |
          if [ -z "${RENDER_WEB_DEPLOY_HOOK_URL}" ]; then
            echo "RENDER_WEB_DEPLOY_HOOK_URL is not set"
            exit 1
          fi

          curl --fail --show-error --silent \
            --request POST \
            "${RENDER_WEB_DEPLOY_HOOK_URL}"
